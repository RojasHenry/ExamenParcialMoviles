<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="HenryRojasexamen.Pages.ResumePage">
    <StackLayout>
        <Frame BackgroundColor="#2279cf" Grid.Column="0" Grid.Row="1">
            <Label x:Name="txtuseractual" Text="Usuario conectado: " TextColor="White"></Label>
        </Frame>
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="70" />
                <RowDefinition Height="70" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

              <StackLayout Grid.Column="0" Grid.Row="0" >
                <Label Text="Nombre:" />
                <Entry x:Name="txtunombre" Text="" IsReadOnly="True"></Entry>
            </StackLayout>
        
            <StackLayout Grid.Column="0" Grid.Row="1" >
                <Label Text="Total a pagar:" />
                <Entry x:Name="txttotal" Text="" IsReadOnly="True"></Entry>
            </StackLayout>
        </Grid>
    </StackLayout>
</ContentPage>
